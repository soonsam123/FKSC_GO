<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.karat.fksc">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Members.MembersActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Championships.ChampionshipActivity"/>
        <activity android:name=".Ranking.RankingActivity"/>
        <activity android:name=".Login.LoginActivity"/>
        <activity android:name=".Profile.ProfileActivity"/>
        <activity android:name=".EditProfile.EditProfileActivity"/>
        <activity android:name=".AddDojo.AddDojoActivity"/>
        <activity android:name=".OtherProfiles.OtherProfileActivity"/>
        <activity android:name=".DojoActivity.DojoActivity"/>

    </application>


</manifest>